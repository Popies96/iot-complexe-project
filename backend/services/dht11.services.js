/*const DataModel = require('../models/dht11.model');

class DataServices {
  static async readDht() {
    try {
      return await DataModel.find({}, { time_period: 1, month: 1, meteorological_indicator: 1,value: 1 });
    } catch (err) {
      throw err;
    }
  }
}

module.exports = DataServices;*/