import 'package:flutter/material.dart';

class generaleControlInfo extends StatelessWidget {
  const generaleControlInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}